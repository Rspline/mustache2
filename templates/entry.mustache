	<div class="entries">
		{{#entry}}
		<div class="entry">
			<h2><a href="{{url}}">{{title}}</a></h2>
			<div class="body">{{body}}</div>
			<div class="date">{{date}}</div>
		</div>
		{{/entry}}
	</div>